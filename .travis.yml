os: linux
dist: xenial
language: cpp
compiler: gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libarchive-dev libboost-filesystem-dev  libboost-program-options-dev libcrypto++-dev libmicrohttpd

branches:
  only:
  - dev

script: make
